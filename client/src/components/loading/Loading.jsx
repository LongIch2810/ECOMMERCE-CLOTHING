import React from "react";
import { createPortal } from "react-dom";
const Loading = () => {
  return (
    <div className="w-10 h-10 m-5 border-4 border-gray-300 rounded-full border-t-primary animate-spin"></div>
  );
};

export default Loading;
